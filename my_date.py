from datetime import timedelta
delita = timedelta(
        days = 50,
        seconds = 27,
        microseconds = 10,
        milliseconds = 29000,
        minutes = 5,
        hours = 8,
        weeks = 2
        )
print(delita)
